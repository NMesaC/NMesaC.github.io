<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://nmesac.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://nmesac.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-08-11T21:02:40+00:00</updated><id>https://nmesac.github.io/feed.xml</id><title type="html">blank</title><subtitle>A website for my ideas and projects! </subtitle><entry><title type="html">How are Cross Attention and Cross Modal Projection Layers Related in Vision-Language Models?</title><link href="https://nmesac.github.io/blog/2025/crossattn-llava-comparison/" rel="alternate" type="text/html" title="How are Cross Attention and Cross Modal Projection Layers Related in Vision-Language Models?"/><published>2025-06-25T00:00:00+00:00</published><updated>2025-06-25T00:00:00+00:00</updated><id>https://nmesac.github.io/blog/2025/crossattn-llava-comparison</id><content type="html" xml:base="https://nmesac.github.io/blog/2025/crossattn-llava-comparison/"><![CDATA[<style>body{font-family:system-ui,sans-serif;line-height:1.6}h1,h2,h3{font-weight:700}h1{color:#05a}h2{color:#07c}h3{color:#39f}a{color:#06c;text-decoration:none}a:hover{text-decoration:underline}code{background:rgba(27,31,35,0.05);padding:2px 4px;border-radius:4px;font-family:ui-monospace,monospace}pre{background:rgba(27,31,35,0.05);padding:1em;border-radius:6px;overflow-x:auto}blockquote{border-left:4px solid #ccc;padding-left:1em;color:#555;font-style:italic}</style> <h1 id="motivation">Motivation</h1> <p>In my research into Vision-Language Models, I had a simple question I had not seen answered before: “Are Cross Attention and Cross Modal Projection Layers equivalent? If not, how are they related?” After doing a bit of light math and reasoning, I have come to believe the answer is: “LLaVA’s Score Matrix computation is a superset of Cross Attention’s. The attention weights for both mechanism have similar interpretations, but LLaVA uses a slightly different computation that has notable consequences”. Here, I will take the time to describe the background behind cross-modality interactions, the proof sketch for why this is true, and some takeaways and conclusions that can be drawn.</p> <h1 id="background">Background</h1> <h2 id="cross-attention-revisited">Cross Attention Revisited</h2> <p>Let us assume that our image input of shape $h \times w$ has been encoded into a vector of shape $X_i \in \mathbb{R}^{n \times d_i}$, where $d_i$ is the embedding dimension for the image. We also assume our text of length $m$ is tokenized and then turned into an embedding of shape $X_t \in \mathbb{R}^{m \times d_t}$, where $d_t$ is the embedding dimension for the text. Usually, we have $n &lt; hw$ since we are encoding our image, but we could also perform this with raw pixels, where $n = hw$. Cross Attention then has a simple inductive bias. We will use our input image to form our queries, and our text input to form our keys and values. Then, our computation follows closely to that of Standard Dot Product Attention.</p> \[\begin{alignat}{2} &amp;Q = X_iW_q \in \mathbb{R}^{n \times d} &amp;&amp;\text{Queries}\\ &amp;K = X_tW_k \in \mathbb{R}^{m \times d} &amp;&amp;\text{Keys}\\ &amp;V = X_tW_v \in \mathbb{R}^{m \times d} &amp;&amp;\text{Values}\\ &amp;S = QK^T / \sqrt{d} \in \mathbb{R}^{n \times m} &amp;&amp;\text{Scores}\\ &amp;A = \text{softmax}(S) \in \mathbb{R}^{n \times m} &amp;&amp;\text{Attention Weights}\\ &amp;O = AV \in \mathbb{R}^{n \times d} &amp;&amp;\text{Output}\\ \end{alignat}\] <p>The result is that the attention weights represent a probability distribution of each encoded pixel over the prompt tokens. This means that, given an encoded pixel, we can find which token has the highest probability of being related to the given encoded pixel. This is a nice interpretation of the attention weights, since then it means that the output of Cross Attention is simply a weighted average (specifically: a Convex Combination due to the softmax operation) of the vectors of the value matrix. In total, this means each pixel tells us how much the pixel should attend to a given text token, and then uses this probability distribution to weigh the importance of all the text tokens that make up the value matrix.</p> <h2 id="cross-modal-projection-layers-llava">Cross Modal Projection Layers (LLaVA)</h2> <p>We assume again our text of length $m$ has been tokenized and then turned into an embedding of shape $X_t \in \mathbb{R}^{m \times d_t}$. Now, we assume our image input has been encoded into a vector of shape $X_i \in \mathbb{R}^{n \times d_i}$. The idea behind Cross Modal Projection Layers is that instead of learning $W_q$ that is specific to the image modality, we can instead convert our image embeddings to have the same feature dimension as our text embeddings, and then pass the transformed image embeddings into an LLM alongside our text embeddings. This is achieved by learning a projection layer $W: \mathbb{R}^{n \times d_i} \mapsto \mathbb{R}^{n \times d_t}$. This method is based on the observation from LiMBeR [1] that the output embedding space of vision models is similar enough to the input embedding space that only a linear projection is needed to transform vision embeddings to language embeddings when working with LLMs. Once the image embedding is produced from the vision model, it is typically prepended to the text embedding, alongside some [IMG] token or embedding to indicate to the LLM when an image is being processed. The rest of the LLM pipeline proceeds as normal.</p> <h2 id="empirical-considerations">Empirical Considerations</h2> <p>An important note is that these methods are quite similar theoretically. However, when we consider implementing these multimodal methods, we come across some important considerations. When working with Cross Modal Projection Layers, we can use various pre-trained vision models and LLMs, and we ONLY have to learn the projection layer. This means we have to learn a cheap projection layer and we can keep expensive components frozen. When using cross attention, however, we have to learn the query matrix $W_q$. This would actually be equivalent if we only had 1 attention module, but recall that modern LLMs use multi-headed attention and usually are comprised of multiple transformer blocks, usually around 32 blocks. Even if we assume a conservative 4 heads used in Multi-Headed attention, we would have to relearn $4 \times 32 = 128$ query matrices! It should be noted that these $W_q$ are not trivially small, since $d = 4096$ is a common embedding dimension seen in these models training. This means we would see $4096 \times n \times 128 = 524288n$ parameters updated for some image embedding length $n$. Though $n$ can be small depending on the vision model used, other Transformer based Vision Models may produce sequences of length $14 \times 14 = 196$, which would balloon the number of parameters that need to be learned further! Furthermore, it would require having a fixed image encoder beforehand, which is less than ideal.</p> <h2 id="guiding-question">Guiding Question</h2> <p>With this in mind, the natural question that arises is: “Are these methods equivalent?” If these methods are equivalent, or at least similar, why would we use Cross Attention over Cross Modal Projection Layers?</p> <h1 id="analysis">Analysis</h1> <h2 id="main-proof-1-computing-score-matrices">Main Proof 1: Computing Score Matrices</h2> <p>We will use $;$ to represent vertical concatenation. We will also assume that $W_q$ and $W_k$ does NOT split across modality lines, meaning that our image and text embeddings are sufficiently similar so this does not happen. Furthermore, we will only be considering the first attention layer for clarity and succinctness. Finally, we will refer to pixels, not patches. We address these assumptions further in the Appendix.</p> <p>Firstly, let us focus on what LLaVA is computing in the first attention layer, as the equivalence to Cross-Attention will be readily visible from there. Assume we compute $H_i = W \cdot g(X_i) \in \mathbb{R}^{n \times d}$, where $g$ is our visual encoder and $W$ is our learned projection matrix. Further assume we have computed the text embedding $X_t \in \mathbb{R}^{m \times d}$, where $d$ is the embedding dimension of our LLM. We then construct our input $X_{tot} = [H_i \ ; \ X_t]$. This means that $X_{tot} \in \mathbb{R}^{(n+m) \times d}$.</p> <p>As above, we assume that $W_q$ and $W_k$ do not decompose across modality lines. Our resultant computation then gives us:</p> \[\begin{alignat}{2} &amp;Q = X_{tot}W_q = [X_i \ ; \ X_t ]W_q = [ X_iW_q \ ; \ X_tW_q] \in \mathbb{R}^{(n+m) \times d}&amp;&amp;\text{Queries}\\ &amp;K = X_{tot}W_k = [X_i \ ; \ X_t ]W_k = [ X_iW_k \ ; \ X_tW_k] \in \mathbb{R}^{(n+m) \times d}&amp;&amp;\text{Keys}\\ \end{alignat}\] <p>To make the following section as clear as possible, assume $W_q = W_k = I_{d \times d}$. Also, we omit the scaling factors. The following results can be quite easily recovered without these assumptions, but this will allow for clarity and brevity.</p> <p>We will now compute the score matrix and get the following result:</p> \[\begin{alignat}{2} &amp;S = QK^T = [ X_i \ ; \ X_t] @ [ X_i \ ; \ X_t]^T = [ X_i \ ; \ X_t] @ [ X_i^T \ | \ X_t^T] = \begin{bmatrix} X_i X_i^T &amp; X_i X_t^T \\ X_t X_i^T &amp; X_t X_t^T \end{bmatrix} \in \mathbb{R}^{(n+m) \times (n+m)} \\ \end{alignat}\] <p>Now, we will focus on what Cross Attention is computing in the first attention layer. If we assume that $W_q = W_k = I_{d \times d}$ for Cross Attention, as we did with LLaVA above, we get the following:</p> \[\begin{alignat}{2} &amp;Q = X_iW_q = X_i \in \mathbb{R}^{n \times d} &amp;&amp;\text{Queries}\\ &amp;K = X_tW_k = X_t \in \mathbb{R}^{m \times d} &amp;&amp;\text{Keys}\\ &amp;S = QK^T = X_iX_t^T \in \mathbb{R}^{n \times m} &amp;&amp; \text{Scores}\\ \end{alignat}\] <p>We can see that LLaVA actually already computes the score matrix of Cross Attention! The top right block matrix computed by LLaVA is identical to what Cross Attention computes for its score matrix. The top left and bottom right block matrices correspond to image embedding self attention score matrices and text embedding self attention score matrices respectively. Finally, the bottom left block matrix represents cross attention, but with text as the query and images as the keys.</p> <h2 id="main-proof-2-the-softmax-operation">Main Proof 2: The Softmax Operation</h2> <p>In the past section, I have only been focusing on the resultant score matrices. We can clearly see for both computations that, independent of the scaling factor, we have not performed the softmax operation to turn our scores into a probability distribution. Thus, we arrive at an interesting point in this discussion and investigation.</p> <p><strong>Although LLaVA computes the cross attention score matrix alongside other matrices, the softmax operation means that the attention results from both processes must be different!</strong></p> <p>The first question that we can ask from this is clear: What are the implications of this fact? Well, we see that cross attention has a clear interpretation. Cross Attention weights represent a probability distribution of each encoded pixel over the prompt tokens. This means that, given an encoded pixel, we can find which token has the highest probability of being related to the given encoded pixel.</p> <p>The computation LLaVA performs has a similar interpretation, but has caveats associated with it. WLOG, assume we look at the first row of the resultant block matrix. The argument would be roughly the same if we look at any row of the vertically concatenated input, whether it was the image or text input, and all that would change is indexing details. We see that for this first row, we are computing a probability distribution of each encoded pixel over both the other encoded pixels AND the prompt tokens. This is represented as $X_i[1:] @ [X_i \ ; \ X_t]^T$.</p> <h2 id="main-proof-3-llava-interpretation-and-implications">Main Proof 3: LLaVA Interpretation and Implications</h2> <p>We now have a full understanding of what Cross Attention and LLaVA are computing for their attention computations, under some assumptions. We take the time to note here that, if we remove the assumptions that $W_q = W_k = I_{d \times d}$, we still get the same result if we make the weaker assumption that both Cross Attention and LLaVA will use the same $W_q$ and $W_k$ matrices.</p> <p>We then see that LLaVA is computing a probability distribution of pixels over the other encoded pixels AND the prompt tokens for the top block matrices. The bottom block matrices are computing a probability distribution of prompt tokens over encoded pixels AND the prompt tokens. We can interpret this as LLaVA doing an “all in one” computation. Whereas some models like FLAMINGO [3] alternate between Cross Attention and Self Attention, LLaVA is simply computing which pixel or token is attended to the most by a given pixel or token.</p> <p>This interpretation has its pros and cons. We see that by treating both the pixels and tokens as queries and keys, we are able to effectively ask the questions “What information does this (pixel/token) need?” and “What information does this (pixel/token) provide?” when performing our attention computation. Compare this to Cross Attention, which only asks “What does this pixel need from the tokens?” since pixels are only used to form queries in Cross Attention. A consequence of this is that, compared to Cross Attention, we are potentially getting more information with our computation. By seeing how pixels attend to other pixels alongside text tokens (and the same done for text tokens), this may allow VLMs to get a better context for the text + image pair, and ultimately have the models perform better overall!</p> <p>Another consequence is that some may argue that the signal present in the Cross Attention computation might be diluted when performing LLaVA’s computation, since instead of only considering 1 pixel with the prompt tokens, we are considering 1 pixel with all the other pixels (including itself) and the prompt tokens, meaning that probability mass is spread out over more pixels/tokens compared to Cross Attention. Unlike the last point made, this argues that the extra computation performed will unnecessarily attenuate our signal, not provide extra context for the model to perform better. This is a valid concern, as by performing attention with large sequence lengths, independent of multimodal considerations, the signal will necessarily be diluted. Despite this concern, we see that LLaVA tends to outperform FLAMINGO! We see that FLAMINGO only achieves a 23.3 on MMVet [4], a popular benchmark for evaluating multimodal models, wheras the 2023 evaluation of LLaVA receives a 28.3 on MMVet for similar parameter counts (roughly 7B)[5].</p> <p>These results are ultimately contradictory. On one hand, we believe that LLaVA’s computation should give the model more information, by allowing pixels to attend to other pixels, and ultimately lead to better performance, which is what we see empirically. However, theoretically, this should not happen, and instead the signal that Cross Attention provides should be diluted out by all the pixels/tokens, meaning a sparser distribution of probability mass. So what is really going on? The issue is actually confounded by multiple variables. LLaVA’s performance over FLAMINGO cannot be solely attributed to architectural differences, and the notion that “our theoretical worries were unfounded in reality”. LLaVA was released in 2023, with access the better open source Language Models and Vision Encoders compared to what FLAMINGO had in 2022. Furthermore, recent VLMs have continuted the cost effective trend that LLaVA set with its architecture. This coupled with better data and other procedural improvements means that comparing FLAMINGO to LLaVA and current VLMs is like comparing apples to oranges. As a result, we have our own version of a signal versus noise problem: Do current models perform better because the LLaVA projection architecture is better, or do current models perform better because we have gotten better data and larger scales? Ultimately, we cannot know without more experimentation and research.</p> <h1 id="conclusions">Conclusions</h1> <h2 id="summary">Summary</h2> <p>In this blog post, we discussed Cross Attention and LLaVA Projection, the empirical reason why LLaVA’s architecture is more appealing, an analysis of how Cross Attention and LLaVA Projection are similar, and why we do not really know whether cross attention or LLaVA Projection are better.</p> <h2 id="future-work">Future Work</h2> <p>Based on the results and ideas discussed here, there are a couple points for future work.</p> <ol> <li> <p>We should experiment to see if image and text embeddings cluster in the way we assume they should. If they overlap as we expect them to (eg. Black Cat maps to an image of a black cat, or at least is quite similar), then our original belief that $W$ does not decompose across modality lines is justified. If not, we can revisit that assumption, and see if we can force these embeddings to map closer to one another.</p> </li> <li> <p>We can try to investigate if the extra computation LLaVA performs compared to Cross Attention is a truly valuable signal or if it is noise that has been overfitted to. We can 0 out different parts of the resultant block matrix at various points of the inference pipeline, and see how that affects results. If the model fails, is it failing because it needs the “noise”, or is there something else going on under the hood? How can we use this to compress LLaVA and make it more efficient?</p> </li> <li> <p>We could investigate improving the image encoder. Though I did not discuss it here, we know that Language Supervised Vision Encoders outperform normal Vision Encoders by large margins. What sort of gains compared to LLaVA can we get if we improve this stage of the pipeline? Will this ultimately affect or address the signal and noise issues we had here?</p> </li> </ol> <h1 id="appendix">Appendix</h1> <h2 id="a-issues-with-multimodal-training">A: Issues with Multimodal Training</h2> <p>As seen in various works regarding training Multimodal Networks [2], different modalities contribute to the loss different amounts, independent of whether or not we pre-process them. Consider a small model that takes in images and a binary label. The model fuses these modalities by concatenating the label to the end of the image, and processing that with a single $W$ matrix. We can see that if we use raw image pixels, the image will contribute far more to the loss than the binary label since the image deals with values from [0,255] and 3 channels per pixel, compared to a single binary value. Furthermore, even if we consider normalizing the image to be within [0,1], pixel values will not be uniformly distributed across images, meaning dark patches in images will contribute far more to the loss than light image patches!</p> <p>This is originally was a relevant point, but I will take the time to discuss why it actually is not necessary, as it is quite interesting. When I originally began writing this post and reasoning about this situation, I believed that the weight matrix would decompose across modality lines for both the queries and keys in LLaVA. However, upon further inspection, since our image embeddings our projected into the input space of the LLM (as per LiMBeR), we would need more experimental data (saved for future work) to show that the image embeddings and text embeddings cluster seperately, or would have reason to split across modality lines.</p> <h2 id="b-what-about-the-whole-transformer">B: What about the whole Transformer?</h2> <p>We effectively only concerned ourselves with the first attention layer, and ignored the entire Transformer. Why did we do this? Realistically, it is for simplicities sake. Implicitly, we believe that the most important parts of the VLM we need to investigate is the Vision Encoder and the Attention Mechanism. Ideally, we think that these are the blocks that, if improved, will lead to better performance overall. The rest of the Transformer architecture consists of a FFNN, which is used to process our feature representations to pass to later blocks to learn new, more abstract feature representations. Ultimately, we should not disregard the rest of the Transformer Architecture, but turning our main focus onto the Attention Mechanism focuses the article and simplifies the proof. In the future, tweaking and understanding these FFNN’s alongside Attention and the Vision Encoder may be beneficial as well!</p> <h2 id="c-pixels-versus-patches">C: Pixels versus Patches</h2> <p>Modern Vision Encoders do patch wise encodings, compared to pixel based encodings. My knowledge of Cross Attention comes mainly from the Latent Diffusion Model literature, where images are compressed into encoded pixels. However, converting pixels into patches for this argument is relatively trivial.</p> <hr/> <h2 id="references">References</h2> <p>[1] Merullo, J., Castricato, L., Eickhoff, C., &amp; Pavlick, E. (2023). Linearly Mapping from Image to Text Space. arXiv preprint arXiv:2209.15162.</p> <p>[2] Wang, W., Tran, D., &amp; Feiszli, M. (2020). What Makes Training Multi-Modal Classification Networks Hard? arXiv preprint arXiv:1905.12681.</p> <p>[3] Alayrac, J. B., Donahue, J., Luc, P., Miech, A., Barr, I., Hasson, Y., … &amp; Simonyan, K. (2022). Flamingo: a visual language model for few-shot learning. Advances in Neural Information Processing Systems, 35, 23716-23736.</p> <p>[4] Yu, W., Yang, Z., Li, L., Wang, J., Lin, K., Liu, Z., Wang, X., &amp; Wang, L. (2024). MM-Vet: Evaluating Large Multimodal Models for Integrated Capabilities. arXiv preprint arXiv:2308.02490.</p> <p>[5] OpenCompass. (2025). Open VLM Leaderboard. Hugging Face Spaces. https://huggingface.co/spaces/opencompass/open_vlm_leaderboard</p> ]]></content><author><name></name></author><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">You Need to Pay Better Attention: Rethinking the Mathematics of Attention Mechanism</title><link href="https://nmesac.github.io/blog/2024/you-need-to-pay-better-attention/" rel="alternate" type="text/html" title="You Need to Pay Better Attention: Rethinking the Mathematics of Attention Mechanism"/><published>2024-07-18T00:00:00+00:00</published><updated>2024-07-18T00:00:00+00:00</updated><id>https://nmesac.github.io/blog/2024/you-need-to-pay-better-attention</id><content type="html" xml:base="https://nmesac.github.io/blog/2024/you-need-to-pay-better-attention/"><![CDATA[<style>body{font-family:system-ui,sans-serif;line-height:1.6}h1,h2,h3{font-weight:700}h1{color:#05a}h2{color:#07c}h3{color:#39f}a{color:#06c;text-decoration:none}a:hover{text-decoration:underline}code{background:rgba(27,31,35,0.05);padding:2px 4px;border-radius:4px;font-family:ui-monospace,monospace}pre{background:rgba(27,31,35,0.05);padding:1em;border-radius:6px;overflow-x:auto}blockquote{border-left:4px solid #ccc;padding-left:1em;color:#555;font-style:italic}</style> <h1 id="overview">Overview</h1> <p>The ultimate goal of my personal investigations would be to contribute to the realm of edge models that are personalized in a way a user actually wants. As a result, this paper caught my interest by promising more efficient and performant versions of the Attention Mechanism, as well as being related to the fundamentals of Transformer technology. I decided to read, summarize and re-implement this paper, as further understanding these attention mechanisms could very well prove useful to my own work down the line.</p> <h1 id="three-important-points">Three Important Points</h1> <h2 id="1--motivation-for-revisiting-attention">1. Motivation for Revisiting Attention</h2> <p>The original “Attention is All You Need” paper introduced the Scaled Dot Product Attention (SDPA) mechanism, but successive work on Transformers has focused mainly on scaling up the approaches outlined in that paper. Furthermore, since increasing scale has been the main focus of most work, we see that models such as GPT4-o or Gemini 1.5 pro have large capabilities, but are prohibitively expensive to train and too large to deploy on edge devices, such as laptops or mobile devices. We also observe that at these large scales, optimizing critical operations will net large gains. Assume a critical function performs 10 matrix multiplications 1 million times. If we can optimize the function to only perform 9 matrix multiplications, then we perform 1 million less matrix multiplications, and therefore use 10% less compute energy and time to run the function!</p> <p>The authors observed that vanilla SDPA suffers from performing more matrix multiplications than needed. The authors leverage these facts to reformulate SDPA to cut down on the number of matrix multiplications and parameters used for Optimized and Efficient Attention. For Super Attention, the authors empirically observed that a linear layer between inputs improves the generalizability of the transformer, and combined Efficient Attention with this observation to make a new attention mechanism.</p> <h2 id="2-optimized-efficient-and-super-attention">2. Optimized, Efficient and Super Attention</h2> <h3 id="optimized-attention">Optimized Attention</h3> <p>Observe that in SDPA, the output of multi head attention will be as follows:</p> \[\begin{aligned} O = \begin{pmatrix}H_1 &amp; H_2 &amp; \dots &amp; H_h \end{pmatrix} W^O \\ = \begin{pmatrix}S_1VW_1^V &amp; S_2VW_2^V &amp; \dots &amp; S_hVW_h^V \end{pmatrix} \begin{pmatrix} W_1^O \\ W_2^O \\ \vdots \\ W_h^O\end{pmatrix} \\ = S_1VW_1^VW_1^O + S_2VW_2^VW_2^O + \dots + S_hVW_h^VW_h^O \end{aligned}\] <p>This assumes that $W_i^O$ is a matrix with rows $(i-1)d_v + 1, \dots, i d_v$ of $W^O$ for $i = 1,2,\dots,h$</p> <p>The authors note that by the rank-nullity theorem, $VW_i^VW_i^O$ will have at most $d_v$ independent columns. This means that instead of performing all these matrix multiplications, we can instead take “slices” of V depending on which head is attending to the sequence and then compute $S_iV_iW_i^O$ instead of $S_iVW_i^VW_i^O$. This clearly cuts down on the amount of matrix multiplications needed as well as the number of parameters stored.</p> <p>Slicing V is equivalent to creating $V_i$ that consist of the columns of $(i-1)d_v + 1, \dots, id_v$ of V for $i = 1,2,\dots, h$.</p> <h3 id="efficient-attention">Efficient Attention</h3> <p>Similarly to Optimized attention, we can observe that for computing the “scores” in SDPA, we compute the following:</p> \[\begin{aligned} Q_i^{'} = Q W_i^Q \text{ and } K_i^{'} = K W_i^{K}\\S_i = \text{softmax}(\frac{Q_i'K_i^{'T}}{\sqrt{d_k}}) \\ = \text{softmax}(\frac{QW_i^Q W_i^K K}{\sqrt{d_k}}) \end{aligned}\] <p>The authors establish that any 2 successive linear transformations are redundant, since 2 linear transformations one after another are equivalent to a single linear transformation. Therefore, the authors propose using the same slicing technique on K (the keys) in the attention mechanism to bypass the need for this extra linear transformation. We then see the score computation instead becomes:</p> \[\begin{aligned} Q_i^{'} = Q W_i^Q \\ S_i = \text{softmax}(\frac{Q_i'K_i^{T}}{\sqrt{d_k}}) \end{aligned}\] <p>where we now have $K_i$ consisting of the columns of $(i-1)d_v + 1, \dots, id_v$ of K for $i = 1,2,\dots, h$</p> <h3 id="super-attention">Super Attention</h3> <p>Super Attention leverages the gains of efficient attention and an empirical observation the authors made to provide a more effective attention mechanism when it comes to learning for Vision and NLP tasks. The authors observed there is a learnable linear transformation between $Q$ and $K^T$ but not between $K^T$ and $V$. In the original formulation of SDPA, this made sense as the attention scores in $S_i$ correspond to the intuition of “how much attention should be paid” to each of the features of each token of $V_i$.</p> <p>Adding a linear layer, which they call $W^A \in \mathbb{R}^{\ell \times \ \ell}$ ($\ell$ is the embedding dimension) improves performance, and has the impact of mixing and aligning values token-wise. Furthermore, if $W^A$ is to be used for generative modelling, the restriction that $W^A$ is upper triangular is sufficient to prevent “lookahead” during learning.</p> <p>We then see that our final formulation for Super Attention is guided by the following equations:</p> <div style="text-align:center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/summaries/super_att-480.webp 480w,/assets/img/summaries/super_att-800.webp 800w,/assets/img/summaries/super_att-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/summaries/super_att.png" class="z-depth-1" width="600px" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <h2 id="3-method-analysis">3. Method Analysis</h2> <h3 id="optimized-attention-1">Optimized Attention</h3> <p>Optimized attention reduces the size of an attention layer by $\frac{1}{4}$, and reduces the cost of a single forward pass through the attention layer by $h$ matrix multiplications, where $h$ is the number of heads. Given the assumption that $d_v = d_k = d_m / h \implies$ there are $d_m^2$ less parameters since standard SDPA has an extra $W_1^V,W_2^V,\dots,W_h^V$ matrix multiplications, each with dimensions $d_md_v$.</p> <h3 id="efficient-attention-1">Efficient Attention</h3> <p>Efficient attention follows the same analysis as Optimized Attention. Now, we reduce the size of the attention layer by $\frac{1}{2}$, perform $2h$ less matrix multiplications on a single forward pass and there are $2d_m^2$ less parameters.</p> <h3 id="super-attention-1">Super Attention</h3> <p>The analysis the paper provides for Super Attention is not as rigorous as that of Optimized and Efficient attention, but they observe that it is roughly similar to that of Optimized attention. We retain at least the same gains of reduction in size of an attention layer by $\frac{1}{4}$ and that the cost of a single forward pass will be reduced by $h$ matrix multiplications at minimum. However, we know that the bounds will be slightly better than this, since $W^A \in \mathbb{R}^{\ell \times \ell}$ where $\ell &lt; d_m$ so we will save on some amount of parameters. Since $\ell$ and $d_k$ aren’t necessarily ordered, the analysis lower bounds the gains instead, as detailed above.</p> <h1 id="most-glaring-deficiency">Most Glaring Deficiency</h1> <p>This paper acknowledges this issue, but it is still a notable issue. The authors are resource limited, so they were not able to measure actual performance on training large models, such as LLAMA or OPT. I think that while there are noticeable gains, even on smaller problems such as IMDB sentiment classification and COCO Image Classification, the only way these edits to attention could be made more popular would be through experiments on training these larger models.</p> <h1 id="conclusions-for-future-work">Conclusions For Future Work</h1> <p>This paper has shown me to truly treat everything the way a scientist would: be skeptical of everything. It is quite easy to accept a popular mechanism such as Self Attention as gospel, but these others analyzed it further and discovered optimizations to be made. As a result, this paper helped give me a better eye for finding places where optimizations could be made in ML Systems and Theory.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Grounding Language Models to Images for Multimodal Inputs and Outputs (FROMAGE)</title><link href="https://nmesac.github.io/blog/2024/grounding-language-models-to-images-for-multimodal-io/" rel="alternate" type="text/html" title="Grounding Language Models to Images for Multimodal Inputs and Outputs (FROMAGE)"/><published>2024-06-13T00:00:00+00:00</published><updated>2024-06-13T00:00:00+00:00</updated><id>https://nmesac.github.io/blog/2024/grounding-language-models-to-images-for-multimodal-io</id><content type="html" xml:base="https://nmesac.github.io/blog/2024/grounding-language-models-to-images-for-multimodal-io/"><![CDATA[<style>body{font-family:system-ui,sans-serif;line-height:1.6}h1,h2,h3{font-weight:700}h1{color:#05a}h2{color:#07c}h3{color:#39f}a{color:#06c;text-decoration:none}a:hover{text-decoration:underline}code{background:rgba(27,31,35,0.05);padding:2px 4px;border-radius:4px;font-family:ui-monospace,monospace}pre{background:rgba(27,31,35,0.05);padding:1em;border-radius:6px;overflow-x:auto}blockquote{border-left:4px solid #ccc;padding-left:1em;color:#555;font-style:italic}</style> <h1 id="motivation">Motivation</h1> <p>In my investigations for Multimodal RAG, I realized that whatever a language transformer and visual transformer attend to should be the same. If they are vectors, they should be similar. This is true, but only if the modalities are the same. This implies that a sentence and image should be similar IFF they are in the same space. Some more research told me this is called “Grounding”. This paper discusses this, and I’ll provide a 3 sentence review after revising it.</p> <h1 id="paper-abstract">Paper Abstract</h1> <p>We propose an efficient method to ground pretrained text-only language models to the visual domain, enabling them to process arbitrarily interleaved image-and-text data, and generate text interleaved with retrieved images. Our method leverages the abilities of language models learnt from large scale text-only pretraining, such as in-context learning and free-form text generation. We keep the language model frozen, and finetune input and output linear layers to enable cross-modality interactions. This allows our model to process arbitrarily interleaved image-and-text inputs, and generate free-form text interleaved with retrieved images. We achieve strong zero-shot performance on grounded tasks such as contextual image retrieval and multimodal dialogue, and showcase compelling interactive abilities. Our approach works with any off-the-shelf language model and paves the way towards an effective, general solution for leveraging pretrained language models in visually grounded settings.</p> <h1 id="three-important-points">Three Important Points</h1> <h2 id="1-the-need-for-visual-inputs">1. The Need for Visual Inputs</h2> <p>The authors note that current Large Language Models are grounded in the language modality, since they are trained on large corpuses of internet text data. However, this means that LLMs are unable to process multimodal data, such as audio or visual data, and are inherently limited in that respect. The authors note that visual data can be information rich and serve to ground LLMs in the real world and make them more effective than just being trained on internet text data. This paper focuses on the incorporation and interleaving of visual data into an pretrained LLM, with a focus on Image Captioning and Image Retrieval given a prompt. This approach is efficient and relevant since their architecture leverages existing visual models and LLM models by learning small weight matrices and embeddings for only a single new token, meaning that instead of training a large costly model, training is relatively fast and efficient.</p> <h2 id="2-fromage-model">2. FROMAGe Model</h2> <p>This paper introduces the FROMAGe model. The FROMAGe model works by taking an existing LLM and Visual Encoder, freezing their parameters, and learning transformations between image and text embedding spaces to perform image $\rightarrow$ text and text $\rightarrow$ image transformations. FROMAGe was designed for image captioning and image retrieval, and does these steps separately. It should be noted that this is possible since a previous paper, LIMBeR, found that learnt representations of vision and language models are equivalent up to a linear transformation, which can be learned as a linear layer.</p> <div style="text-align:center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/summaries/image_captioning-480.webp 480w,/assets/img/summaries/image_captioning-800.webp 800w,/assets/img/summaries/image_captioning-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/summaries/image_captioning.jpg" class="z-depth-1" width="600px" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <p>For Image Captioning, FROMAGe learns a single linear layer called $W_C$ that serves to translate the output of the visual encoder into a token that is then prepended to the caption text corresponding to the image. This lines up with previous work on image captioning, which generates text tokens conditioned on a visual prefix. The standard log likelihood loss is used to learn the linear layer.</p> <div style="text-align:center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/summaries/image_text_retrieval-480.webp 480w,/assets/img/summaries/image_text_retrieval-800.webp 800w,/assets/img/summaries/image_text_retrieval-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/summaries/image_text_retrieval.jpg" class="z-depth-1" width="600px" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <p>For image retrieval, FROMAGe learns 2 separate linear layers, called $W_t, W_i$ for translating text $\rightarrow$to image and image $\rightarrow$ text respectively. The authors used the InfoNCE loss and Contrastive Learning to learn these linear layers. Notably, the authors also learned a new <code class="language-plaintext highlighter-rouge">[RET]</code> token and the associated embeddings. This allows FROMAGe to be able to produce a stronger text representation for the image as well as be able to generate the new <code class="language-plaintext highlighter-rouge">[RET]</code> token at inference time to allow for the interleaving of images and text.</p> <h2 id="3-fromage-vs-clip">3. FROMAGe vs CLIP</h2> <p>The authors perform various experiments comparing FROMAGe to other models, most notably CLIP. CLIP functions by learning a correspondence between which texts and images were paired together during training time. CLIP is a powerful model, but an admittedly rigid and inflexible one, since instead of learning translation layers like FROMAGe does, CLIP learns pairings of data and solely relies on in context training to function. The experiments support this intuition, as they show that given more context than just 1 caption, FROMAGe outperforms CLIP and other related models at recall at K.</p> <p>Recall at K is a metric that measures, given K recommendations from the model, the proportion of relevant results in these top K recommendations compared to the number of relevant results in the dataset.</p> <p>Further experiments show that FROMAGe performs well in visual dialogue tasks and in general, effectively leverages the visual data that it has access to that other LLM or mixed-modality models do not effectively use. FROMAGe, when provided with both more visual and text input, increases its recall at 1 (R @ 1) measure, which indicates that it does better at finding the most relevant answer from the given dataset (Visual Storytelling dataset).</p> <div style="text-align:center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/summaries/model_exp_1-480.webp 480w,/assets/img/summaries/model_exp_1-800.webp 800w,/assets/img/summaries/model_exp_1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/summaries/model_exp_1.jpg" class="z-depth-1" width="600px" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div style="text-align:center"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/summaries/model_exp_2-480.webp 480w,/assets/img/summaries/model_exp_2-800.webp 800w,/assets/img/summaries/model_exp_2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/summaries/model_exp_2.jpg" class="z-depth-1" width="600px" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <h1 id="most-glaring-deficiency">Most Glaring Deficiency</h1> <p>The authors did not show the effect of just providing more images to FROMAGe to understand empirically how much more information a single image provides to the model, and if there are diminishing returns on providing more images or not. In the above results, we notice that the authors provide more captions and images at the same time, which makes it difficult to tell if text and image data need to strictly be coupled to provide better performance, or if just providing more image data can improve the R @ k metric.</p> <h1 id="conclusions-for-future-work">Conclusions for Future Work</h1> <p>FROMAGe introduces some of the machineries and frameworks for thinking about and leveraging more general multimodal models. The notion of multimodality leveraging translations between pre-trained representations instead of explicitly training to learn which images and sentences were paired together, like CLIP, motivate more general architectures that can be used for more general use cases in the future.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[]]></summary></entry></feed>